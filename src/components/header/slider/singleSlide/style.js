import { makeStyles } from "@mui/styles";

const useStyles = makeStyles({
  root: {
    // overflow: "hidden",

    width: "250px",
    height: "auto",
    margin: "10px",
  },
  img: {
    width: "100%",
    height: "auto",
    objectFit: "contain",
  },
});

export default useStyles;
